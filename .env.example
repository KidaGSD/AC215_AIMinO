# AIMinO Environment Configuration

# Required: Google Gemini API Key
GEMINI_API_KEY=your_api_key_here

# Server settings
AIMINO_SERVER_PORT=8000
AIMINO_API_PREFIX=/api/v1
SERVER_URL=http://127.0.0.1:8000

# Development settings
AIMINO_RELOAD=0                    # Set to 1 for hot-reload during development

# Data storage
AIMINO_DATA_ROOT=~/AIMINO_DATA     # Where datasets and cache are stored

# Image loading settings (for large images >16k pixels)
AIMINO_AUTO_DOWNSAMPLE=2           # Auto-downsample factor: 2=2x, 4=4x (recommended for large images)
AIMINO_SKIP_16K_CHECK=1            # Skip 16k pixel check (set to 1 if using auto-downsample)
AIMINO_AUTLOAD_MAX_BYTES=2000000000  # Max file size for auto-load (2GB)
AIMINO_DISABLE_AUTOLOAD=0          # Set to 1 to disable auto-loading entirely
