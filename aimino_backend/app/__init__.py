"""FastAPI app placeholder (no routes yet).

Routes to add:
  - POST /invoke  -> run Lead Manager and return final_commands
  - GET  /healthz -> return status, schema version
"""

# from fastapi import FastAPI

# app = FastAPI()

# TODO: wire settings, logging, schemas, and lead manager runner here.

