[project]
name = "aimino-api-service"
version = "0.1.0"
description = "AIMinO FastAPI service (cheese-style)"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pydantic>=2",
  "pydantic-settings>=2",
  "httpx",
]

[tool.setuptools]
packages = []

